import React from 'react';
import styled from 'styled-components';

const SummaryBlock = () => {
  return (
    <div>I'm some sort of div with asome sort of stying.</div>
  )
}

export default SummaryBlock;